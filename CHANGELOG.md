# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-09-18

首个公开版本。

### 视觉

* 整屏由单个片元着色器程序化绘制：深蓝底色 + 48px 青色网格 + 三团椭圆辉光，
  以及完整的眼睛（外圈蓝盘、四角眼睫、眼白 / 虹膜 / 瞳孔六层呼吸缩放、高光、
  229 条横向光丝、扩散脉冲环）。
* 与参考实现逐像素比对：全屏平均绝对差 4.4/255，眼睛圆盘、眼白、外光晕区域
  的 RGB 差异在 1–3 以内。
* 复刻参考项目的 1440ms 主时钟与 `cubic-bezier(.72,0,.28,1)` 缓动，四层眼球的
  相位差（0 / 45 / 90 / 180 ms）完全一致。
* 周期性故障撕裂：`threads`（横向噪声位移）与 `blocks`（5 段横条错位）交替。

### 屏保功能

* 支持 Windows 屏保标准参数：`/s` 运行、`/c` 配置、`/p <hwnd>` 缩略预览。
* 多显示器：每块屏一条独立交换链，默认只在主屏显示，可切到全部显示器。
* 退出条件为「点击或按键」——移动鼠标不退出（理由见 README）。

### 摄像头注视

* 人脸检测走系统自带的 `Windows.Media.FaceAnalysis`，零模型文件；检测不到人脸
  时退回帧差运动检测，两级都没命中则保持最后位置。
* 由人脸包围盒高度反推距离（成年人的脸约 200mm），驱动「接近 / 离开」双阈值
  迟滞与注视纵深。
* 眼睛固定在所在屏幕正中，瞳孔按真实几何（`tan = 横向 / 纵深`）转向人物并做
  饱和处理。
* 人物走近到「接近距离」以内时切换到离他最近的屏幕，跨屏叠双层故障撕裂 +
  一次完整眨眼。
* 「半睁眼」确认动画约 5.2 秒，之后 0.25 秒快速睁回。

### 设置界面

* 纯 Win32 构建，`comctl32 v6` 清单 + 逐显示器 DPI 感知 + DWM 圆角。
* 多显示器布局图：橙点标摄像头（可点在屏幕缝隙上），红点实时显示检测到的人，
  连线即视线方向；边上的偏航 / 俯仰 / 水平视野用于视角校正闭环。
* 窗口自适应屏幕大小，过小时出现滚动条。

### 诊断

* `cargo test` —— 16 项几何 / 检测 / 状态机单元测试。
* `/d` 诊断模式：显示器、显卡、摄像头清单，并逐路摄像头抓一帧把检测结果画在
  图上（绿 = 人脸 / 橙 = 运动 / 红 = 无）。
* `/f <图片> [输出]`：对单张图片跑整条检测链，并打印端到端的注视落点。
* `[logging] log_enabled` 日志开关，默认关闭。

### 已知限制

见 README「已知限制」一节。

[1.0.0]: https://github.com/Attect/FairyScreenSaver/releases/tag/v1.0.0
